Name cpipe

Usage {copy stdin to stderr while counting bytes and reporting progess}


Int -b bsize {buffer size in kB} \
    range=1,oo \
    default=128

Flag -vt vt {show throughput}

Flag -vr vr {show read-times}

Flag -vw vw {show write-times}

